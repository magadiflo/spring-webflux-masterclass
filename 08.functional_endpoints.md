# 🛣️ Sección 08: Functional Endpoints

---

## ⚙️ Configuración inicial

- Creamos el directorio correspondiente a la sección `/sec08` en `webflux-playground`.
- En el archivo `application.yml` definimos la sección activa:
  ````yml
  section: sec08
  ````

---

## 📖 Introducción

Tradicionalmente, hemos expuesto nuestras APIs utilizando controladores anotados, una aproximación que nos resulta
familiar y funciona muy bien. Por ejemplo, este es un controlador típico con anotaciones como `@GetMapping`:

````java

@GetMapping(path = "/{customerId}")
public Mono<ResponseEntity<CustomerResponse>> getCustomer(@PathVariable Long customerId) {
    return this.customerService.getCustomer(customerId)
            .map(ResponseEntity::ok);
}
````

### 🚀 Functional Endpoints en Spring WebFlux

Sin embargo, `Spring WebFlux` también ofrece una alternativa basada en `programación funcional`:
`los Functional Endpoints`.

Con este enfoque:

- La lógica de enrutamiento se define mediante `funciones puras`, declaradas como beans.
- Los endpoints se configuran explícitamente y se asocian a funciones que manejan la solicitud.
- Podemos incluso definir manejadores de errores dentro del mismo flujo funcional.

Ejemplo de definición de rutas:

````bash
route()
  .GET("/api/v1/customers", handler::getAllCustomers)
  .GET("/api/v1/customers/{customerId}", handler::getCustomer)
  .POST("/api/v1/customers", handler::saveCustomer)
  ...
  .onError(CustomerNotFoundException.class, this::badRequestHandler)
  .build();
````

![01.png](assets/section-08/01.png)

### 📌 Diferencias clave

Al usar `Functional Endpoints`:

- Definimos rutas que redirigen a funciones `(HandlerFunction)` que procesan la solicitud.
- Estas funciones extraen datos, invocan la capa de servicio y retornan respuestas.
- El enrutamiento y la lógica de negocio quedan `separados y más explícitos`.

### ⚠️ Nota importante

> El uso de `Functional Endpoints es completamente opcional`.  
> Podemos seguir utilizando controladores anotados `(@RestController)` si ese enfoque se adapta mejor a nuestras
> necesidades.

### 🌟 Ventajas de los Functional Endpoints

- Mayor flexibilidad: control granular del flujo de solicitudes.
- Apropiado para aplicaciones pequeñas o personalizadas, donde las rutas se definen de forma explícita.
- Separación clara entre enrutamiento y lógica de negocio, lo que facilita el mantenimiento.

## 🛠️ Router Configuration

En el paquete `sec08` copiamos el contenido del paquete `sec06`, pero **sin incluir** los directorios `advice` ni el
`controller`. Además, creamos un nuevo directorio llamado `router` y dentro de él definimos la clase de configuración
`CustomerRouter`.

![02.png](assets/section-08/02.png)

### 📌 Clase de configuración `CustomerRouter`

Nuestra clase expone un bean del tipo `RouterFunction`, el cual será implementado en el siguiente apartado.

Equivalente a una clase anotada con `@RestController`, aquí **solo se definen las rutas**.  
La diferencia es que en lugar de usar anotaciones como `@GetMapping` o `@PostMapping`, declaramos las rutas de forma
funcional y explícita.

Por ahora retornamos `null` y dejamos la estructura preparada; más adelante realizaremos la implementación.

````java

@Configuration
public class CustomerRouter {
    @Bean
    public RouterFunction<ServerResponse> customerRoutes() {
        return null;
    }
}
````

#### 🔍 Explicación

- `CustomerRouter`. Es la clase de configuración que contendrá todas las rutas funcionales relacionadas con `Customer`.
- `RouterFunction<ServerResponse>`. Define cómo las solicitudes HTTP se enrutan hacia funciones que generan una
  respuesta `(ServerResponse)`.
- `Bean de configuración`. Al exponerlo como un `@Bean`, Spring lo registra en el contexto y lo utiliza para manejar
  las rutas de la aplicación.
- `Estructura inicial`. En este punto el método retorna `null`, pero en el siguiente paso implementaremos las rutas
  reales (`GET`, `POST`, etc.) que apuntarán a nuestros handlers.

## 🧩 Request Handler: `CustomerHandler`

En el `CustomerHandler` vive la lógica equivalente a los métodos del controlador: `recibir la request`,
`validar datos de entrada` y `delegar al servicio`. En otras palabras, el `CustomerHandler` cumple el mismo rol que
un `@RestController`, pero en el mundo de los **functional endpoints**.

### 🧱 Código completo del `CustomerHandler`

````java

@Slf4j
@RequiredArgsConstructor
@Component
public class CustomerHandler {

    private final CustomerService customerService;

    public Mono<ServerResponse> allCustomers(ServerRequest request) {
        return this.customerService.getAllCustomers()
                .doOnNext(customerResponse -> log.info("{}", customerResponse))
                .as(customerResponseFlux ->
                        ServerResponse.ok().body(customerResponseFlux, CustomerResponse.class));
    }

    public Mono<ServerResponse> getCustomer(ServerRequest request) {
        return this.customerService.getCustomer(this.customerId(request))
                .flatMap(customerResponse ->
                        ServerResponse.ok().bodyValue(customerResponse));
    }

    public Mono<ServerResponse> saveCustomer(ServerRequest request) {
        return request.bodyToMono(CustomerRequest.class)
                .transform(CustomerValidator.validate())
                .flatMap(this.customerService::saveCustomer)
                .flatMap(customerResponse ->
                        ServerResponse.status(HttpStatus.CREATED).bodyValue(customerResponse));
    }

    public Mono<ServerResponse> updateCustomer(ServerRequest request) {
        return request.bodyToMono(CustomerRequest.class)
                .transform(CustomerValidator.validate())
                .flatMap(customerRequest ->
                        this.customerService.updateCustomer(this.customerId(request), customerRequest))
                .flatMap(customerResponse ->
                        ServerResponse.ok().bodyValue(customerResponse));
    }

    public Mono<ServerResponse> deleteCustomer(ServerRequest request) {
        return this.customerService.deleteCustomer(this.customerId(request))
                .then(ServerResponse.noContent().build());
    }

    private long customerId(ServerRequest request) {
        return Long.parseLong(request.pathVariable("customerId"));
    }
}
````

### 🔍 Helper privado `customerId(...)`

El método `customerId(...)` centraliza la extracción del path variable para no repetir:

````bash
Long.parseLong(request.pathVariable("customerId"))
````

> ⚠️ **Nota:**
> - El `RouterFunction` garantiza que el path variable `{customerId}` **existe** (si no existiera, el router retorna un
    `404` antes de llegar al handler).
> - Pero `no garantiza` que sea un valor numérico válido.
> - Una petición como `PUT /api/v1/customers/abc` llegaría al handler y lanzaría un `NumberFormatException`, que debe
    ser manejado por un `WebExceptionHandler` global.

### 🔄 El operador `.as()`

El método `allCustomers()` utiliza el operador `.as()`. Este operador es básicamente una **transformación de nivel
superior**. Su firma para un publisher del tipo flux es:

````bash
public final <P> P as(Function<? super Flux<T>, P> transformer)
````

- Recibe el `Flux<T>` como argumento de una función transformadora, permitiéndote operar sobre él de forma directa.
- Retorna lo que esa función devuelva, que puede ser cualquier tipo `P`, no necesariamente otro `Flux` o `Mono`.
- El `P` se `infiere automáticamente` por el compilador según lo que retorne la función lambda que le pasemos.

Ejemplo en nuestro caso:

````bash
.as(customerResponseFlux ->
    ServerResponse.ok().body(customerResponseFlux, CustomerResponse.class))
//  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
//  Esto retorna Mono<ServerResponse>, entonces P = Mono<ServerResponse>
````

👉 Como `ServerResponse.ok().body(...)` retorna `Mono<ServerResponse>`, el compilador infiere que
`P = Mono<ServerResponse>`. De esta forma, el método `allCustomers()` puede retornar directamente
`Mono<ServerResponse>`.

### 📌 Equivalente sin `.as()`

El mismo código podría escribirse sin `.as()`:

````java
public Mono<ServerResponse> allCustomers(ServerRequest request) {
    Flux<CustomerResponse> flux = this.customerService.getAllCustomers();
    return ServerResponse.ok().body(flux, CustomerResponse.class);
}
````

- `.as()` simplemente permite mantener un estilo fluent/encadenado sin romper la cadena de operadores reactivos.
- Tanto `Flux` como `Mono` tienen su propio `.as()` con el mismo principio.

## 📘 Configuración de rutas funcionales (Functional Endpoints)

Como dijimos hace dos lecciones, la clase `CustomerRouter` es equivalente a una clase anotada con `@RestController`:  
aquí **solo se definen las rutas**.

La diferencia es que en lugar de usar anotaciones como `@GetMapping` o `@PostMapping`, declaramos las rutas de forma
**funcional y explícita**.

### 🎯 Objetivo

En esta clase establecemos las rutas `HTTP` básicas (`GET`, `POST`, `PUT`, `DELETE`) aplicando una separación clara
entre:

- **Definición de rutas** → responsabilidad de `CustomerRouter`.
- **Lógica de manejo** → responsabilidad de `CustomerHandler`.

Además, se utiliza una constante base `CUSTOMERS_URI` para asegurar consistencia y evitar duplicación de cadenas de
texto en la definición de rutas.

### ✅ Beneficios de esta estrategia

- 📑 **Facilita el mantenimiento y la refactorización** del código.
- 👀 **Mejora la legibilidad y claridad** de las rutas definidas.
- 🧩 **Buenas prácticas de nombramiento semántico**, evitando cadenas mágicas repetidas.

### 🧱 Clase `CustomerRouter`

````java

@Configuration
public class CustomerRouter {

    private static final String CUSTOMERS_URI = "/api/v1/customers";
    private static final String CUSTOMER_ID_PATH = "/{customerId}";

    @Bean
    public RouterFunction<ServerResponse> customerRoutes(CustomerHandler handler) {
        return RouterFunctions.route()
                .GET(CUSTOMERS_URI, handler::allCustomers)
                .GET(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::getCustomer)
                .POST(CUSTOMERS_URI, handler::saveCustomer)
                .PUT(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::updateCustomer)
                .DELETE(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::deleteCustomer)
                .build();
    }
}
````

#### 🔍 Explicación

- Constantes `CUSTOMERS_URI` y `CUSTOMER_ID_PATH`. Evitan duplicación de cadenas y aseguran consistencia en la
  definición de rutas.
- `RouterFunctions.route()`. Método builder que permite declarar las rutas de forma encadenada y explícita.
- Delegación a `CustomerHandler`. Cada ruta se asocia directamente a un método del handler (`handler::allCustomers`,
  `handler::getCustomer`, etc.).
- `.build()`. Finaliza la construcción del `RouterFunction` y lo expone como bean para que Spring lo registre en el
  contexto.

## 🧪 Probando Functional Endpoints

Una vez configuradas nuestras rutas en `CustomerRouter`, es momento de verificar que los **endpoints CRUD** funcionan
correctamente. Para ello realizamos peticiones HTTP con `curl` y observamos las respuestas.

### 📋 1. Listar todos los clientes

````bash
$ curl -v http://localhost:8080/api/v1/customers | jq
>
< HTTP/1.1 200 OK
< transfer-encoding: chunked
< Content-Type: application/json
<
[
  {
    "id": 1,
    "name": "sam",
    "email": "sam@gmail.com"
  },
  {...},
  {
    "id": 10,
    "name": "ethan",
    "email": "ethan@example.com"
  }
]
````

✅ El endpoint retorna un `200 OK` con un arreglo JSON de clientes.

### 🔎 2. Obtener detalle de un cliente

````bash
$ curl -v http://localhost:8080/api/v1/customers/5 | jq
>
< HTTP/1.1 200 OK
< Content-Type: application/json
< Content-Length: 53
<
{
  "id": 5,
  "name": "sophia",
  "email": "sophia@example.com"
}
````

✅ El endpoint retorna un `200 OK` con el detalle del cliente solicitado.

### ➕ 3. Guardar un cliente

````bash
$ curl -v -X POST -H "Content-Type: application/json" -d "{\"name\": \"Lesly\", \"email\": \"lesly@gmail.com\"}" http://localhost:8080/api/v1/customers | jq
>
< HTTP/1.1 201 Created
< Content-Type: application/json
< Content-Length: 50
<
{
  "id": 11,
  "name": "Lesly",
  "email": "lesly@gmail.com"
}
````

✅ El endpoint retorna un `201 Created` con el cliente recién creado.

### ✏️ 4. Actualizar un cliente

````bash
$ curl -v -X PUT -H "Content-Type: application/json" -d "{\"name\": \"Milagros\", \"email\": \"milagros@gmail.com\"}" http://localhost:8080/api/v1/customers/11 | jq
>
< HTTP/1.1 200 OK
< Content-Type: application/json
< Content-Length: 56
<
{
  "id": 11,
  "name": "Milagros",
  "email": "milagros@gmail.com"
}
````

✅ El endpoint retorna un `200 OK` con el cliente actualizado.

### 🗑️ 5. Eliminar un cliente

````bash
$ curl -v -X DELETE http://localhost:8080/api/v1/customers/11 | jq
>
< HTTP/1.1 204 No Content
<
````

✅ El endpoint retorna un `204 No Content`, confirmando que el cliente fue eliminado.

### 📌 Conclusión

- Los functional endpoints CRUD (GET, POST, PUT, DELETE) funcionan correctamente.
- Cada operación devuelve el status code apropiado (200, 201, 204) y el body esperado en formato JSON.
- Con esto validamos que la configuración de rutas en `CustomerRouter` y la lógica en `CustomerHandler`
  están integradas y operativas.

## ⚠️ Validación de Entradas / Manejador de Errores

En esta sección trabajamos el manejo de errores cuando un `handler function` emite una **señal de error**  
(es decir, un `Mono.error(...)`).

Esto es similar a lo que hacíamos con la clase anotada con `@RestControllerAdvice`, pero adaptado al enfoque funcional
con `RouterFunction` en `Spring WebFlux`.

### 🎯 Estrategia de manejo de errores

En este curso implementamos dos niveles de manejo:

1. **Manejo de errores a nivel de Router** → usando `.onError(...)` en `CustomerRouter`.
2. **Manejo de errores global** → mediante una clase que extiende `AbstractErrorWebExceptionHandler`.

De esta forma, capturamos tanto excepciones específicas como aquellas que no estén contempladas en el router.

### 🧩 Manejador de Excepciones Personalizado

La clase `CustomerExceptionHandler` implementa métodos que retornan un `ServerResponse` con la excepción envuelta en un
**ProblemDetail** (formato estándar de errores HTTP definido por **RFC 7807**).

Para evitar duplicación de código, se crea un método privado `buildResponse(...)` que encapsula la lógica común y recibe
un `Consumer<ProblemDetail>` para personalizar cada caso.

````java

@Slf4j
@Component
public class CustomerExceptionHandler {
    public Mono<ServerResponse> handleNotFoundException(CustomerNotFoundException ex, ServerRequest request) {
        log.error(ex.getMessage());

        return this.buildResponse(HttpStatus.NOT_FOUND, ex, request, problemDetail -> {
            problemDetail.setType(URI.create("https://example.com/problems/customer-not-found"));
            problemDetail.setTitle("Cliente no encontrado");
        });
    }

    public Mono<ServerResponse> handleInvalidInputException(InvalidInputException ex, ServerRequest request) {
        log.error(ex.getMessage());

        return this.buildResponse(HttpStatus.BAD_REQUEST, ex, request, problemDetail -> {
            problemDetail.setType(URI.create("https://example.com/problems/invalid-input"));
            problemDetail.setTitle("Entrada no válida");
        });
    }

    private Mono<ServerResponse> buildResponse(HttpStatus status, Exception ex, ServerRequest request,
                                               Consumer<ProblemDetail> problemDetailConsumer) {
        ProblemDetail problemDetail = ProblemDetail.forStatusAndDetail(status, ex.getMessage());
        problemDetail.setInstance(URI.create(request.path()));

        problemDetailConsumer.accept(problemDetail);

        return ServerResponse
                .status(status)
                .contentType(MediaType.APPLICATION_PROBLEM_JSON)
                .bodyValue(problemDetail);
    }
}
````

### 🛣️ Manejo de errores a nivel de Router

En `CustomerRouter` inyectamos el `CustomerExceptionHandler` y usamos `.onError(...)` para asociar cada tipo
de excepción con su manejador.

````java

@RequiredArgsConstructor
@Configuration
public class CustomerRouter {

    private static final String CUSTOMERS_URI = "/api/v1/customers";
    private static final String CUSTOMER_ID_PATH = "/{customerId}";

    private final CustomerExceptionHandler customerExceptionHandler;

    @Bean
    public RouterFunction<ServerResponse> customerRoutes(CustomerHandler handler) {
        return RouterFunctions.route()
                .GET(CUSTOMERS_URI, handler::allCustomers)
                .GET(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::getCustomer)
                .POST(CUSTOMERS_URI, handler::saveCustomer)
                .PUT(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::updateCustomer)
                .DELETE(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::deleteCustomer)
                .onError(CustomerNotFoundException.class, this.customerExceptionHandler::handleNotFoundException)
                .onError(InvalidInputException.class, this.customerExceptionHandler::handleInvalidInputException)
                .build();
    }
}
````

#### 🔎 ¿Qué es `.onError(...)` en el Router?

- `Propio del RouterFunction`:
    - Este método no pertenece a `Flux` ni `Mono` como `onErrorResume` o `onErrorReturn`.
    - Es parte del `builder` de rutas funcionales `(RouterFunctions.route())`, y su propósito es asociar un tipo de
      excepción con un manejador específico.
- `Funciona como un "catch" declarativo`:
    - Cuando un `HandlerFunction` lanza una excepción (por ejemplo, `Mono.error(new CustomerNotFoundException(...))`),
      el router intercepta esa señal de error y redirige la ejecución al manejador que definimos con `.onError(...)`.
- Diferencia con operadores reactivos (`onErrorResume`, `onErrorReturn`):
    - `onErrorResume` y `onErrorReturn` son operadores de reactores `(Flux/Mono)`, usados dentro de un flujo reactivo
      para recuperar o transformar un error.
    - `.onError(...)` en el router es un mapeo de excepciones a handlers a nivel de enrutamiento, no dentro del flujo
      de datos.
    - Es decir, en lugar de manejar el error dentro del `Mono` o `Flux`, lo manejamos en el pipeline de enrutamiento.
- `Firma simplificada`:
    ````java
    <T extends Throwable> Builder onError(Class<T> exceptionType,
                                          BiFunction<T, ServerRequest, Mono<ServerResponse>> handler);
    ````
    - `exceptionType`: la clase de la excepción que queremos capturar.
    - `handler`: función que recibe la excepción y el `ServerRequest`, y retorna un `Mono<ServerResponse>` con la
      respuesta adecuada.

👉 En resumen:
> `.onError(...)` es un mecanismo propio del router para capturar excepciones y redirigirlas a un manejador de errores,
> funcionando como un “catch declarativo” a nivel de enrutamiento. No reemplaza a `onErrorResume` o `onErrorReturn`,
> sino que complementa el manejo de errores en el contexto de functional endpoints.

### 🌐 Manejo de errores global

La clase `GlobalErrorHandler` extiende `AbstractErrorWebExceptionHandler` y captura excepciones no manejadas en
el router. Se registra una única ruta comodín que delega a un método central, el cual decide qué handler específico
invocar según el tipo de excepción.

````java

@Slf4j
@Order(-2)
@Component
public class GlobalErrorHandler extends AbstractErrorWebExceptionHandler {

    // Spring inyecta tod esto automáticamente
    public GlobalErrorHandler(ErrorAttributes errorAttributes,
                              WebProperties webProperties,
                              ApplicationContext applicationContext,
                              ServerCodecConfigurer configurer) {
        super(errorAttributes, webProperties.getResources(), applicationContext);

        // necesario para que Spring sepa cómo serializar las respuestas
        super.setMessageWriters(configurer.getWriters());
        super.setMessageReaders(configurer.getReaders());
    }

    @Override
    protected RouterFunction<ServerResponse> getRoutingFunction(ErrorAttributes errorAttributes) {
        return RouterFunctions.route(RequestPredicates.all(), this::handleAllErrors);
    }

    private Mono<ServerResponse> handleAllErrors(ServerRequest request) {
        Throwable throwable = this.getError(request);
        return switch (throwable) {
            case NumberFormatException ex -> this.handleNumberFormatException(ex, request);
            default -> this.handleGenericException(throwable, request);
        };
    }

    private Mono<ServerResponse> handleNumberFormatException(NumberFormatException ex, ServerRequest request) {
        log.error(ex.getMessage());

        return this.buildResponse(HttpStatus.BAD_REQUEST, ex, request, problemDetail -> {
            problemDetail.setType(URI.create("https://example.com/problems/invalid-number-format"));
            problemDetail.setDetail("Se esperaba un valor numérico pero se recibió un valor no válido");
            problemDetail.setTitle("Entrada no válida");
        });
    }

    private Mono<ServerResponse> handleGenericException(Throwable ex, ServerRequest request) {
        log.error("Excepción no controlada en [{}]", request.path(), ex);

        return this.buildResponse(HttpStatus.INTERNAL_SERVER_ERROR, ex, request, problemDetail -> {
            problemDetail.setType(URI.create("https://example.com/problems/internal-error"));
            problemDetail.setTitle("Internal Server Error");
        });
    }

    private Mono<ServerResponse> buildResponse(HttpStatus status, Throwable ex, ServerRequest request,
                                               Consumer<ProblemDetail> problemDetailConsumer) {
        ProblemDetail problemDetail = ProblemDetail.forStatusAndDetail(status, ex.getMessage());
        problemDetail.setInstance(URI.create(request.path()));

        problemDetailConsumer.accept(problemDetail);

        return ServerResponse
                .status(status)
                .contentType(MediaType.APPLICATION_PROBLEM_JSON)
                .bodyValue(problemDetail);
    }
}
````

#### 📌 ¿Por qué extender `AbstractErrorWebExceptionHandler`?

`Spring WebFlux` ya trae un manejador de errores por defecto, pero si queremos personalizar las respuestas globales
(por ejemplo, devolver un `ProblemDetail` en lugar de un `JSON genérico`), necesitamos sobrescribir ese comportamiento.
La clase `AbstractErrorWebExceptionHandler` es la base que Spring ofrece para construir un error handler global en
aplicaciones `WebFlux`.

#### 📌 Analizando código

- `@Order(-2)` → define la prioridad de este manejador.
    - Spring tiene su propio `DefaultErrorWebExceptionHandler` con orden `-1`.
    - Al poner `-2`, nuestro handler se ejecuta antes que el de Spring, asegurando que nuestras respuestas
      personalizadas tengan prioridad.
- `Constructor`: Parámetros inyectados automáticamente por Spring.
    - `ErrorAttributes` → metadatos del error (mensaje, timestamp, etc.).
    - `WebProperties` → configuración web de la aplicación.
    - `ApplicationContext` → acceso al contexto de Spring.
    - `ServerCodecConfigurer` → define cómo serializar y deserializar las respuestas (codecs).

👉 **Sí, es cierto: Spring inyecta todo esto automáticamente en el constructor.**

- `super(...)` → inicializa la clase base con los atributos necesarios.
- `setMessageWriters` y `setMessageReaders` → muy importantes:
    - Indican a Spring cómo serializar las respuestas (JSON, ProblemDetail, etc.).
    - Si no configuramos esto, Spring no sabría cómo convertir nuestro `ProblemDetail` en JSON y devolverlo al cliente.
    - En la práctica, sin estos setters, obtendríamos errores de serialización o respuestas vacías.


- Método principal: `getRoutingFunction(...)`
    - Este método define **cómo se enrutan las excepciones globales.**
    - `AbstractErrorWebExceptionHandler` requiere obligatoriamente al menos una ruta registrada con `GET()`,
      `POST()`, etc. Sin ella, el builder lanza un `IllegalStateException` al arrancar.
    - Sin embargo, en lugar de registrar una ruta por cada método HTTP (`GET("/**")`, `POST("/**")`,
      `PUT("/**")`, etc.), se usa `RequestPredicates.all()` como predicado comodín único que acepta cualquier método
      HTTP y cualquier path.
    - Toda petición de error es delegada al método `handleAllErrors(...)`, que centraliza la decisión de qué handler
      invocar según el tipo de excepción.
- Método `handleAllErrors(...)`
    - Recupera la excepción almacenada por Spring mediante `this.getError(request)`.
    - Usa un `switch` con **pattern matching (Java 21+)** para despachar al handler específico según el tipo
      de excepción:
        - `NumberFormatException` → errores de conversión numérica (ejemplo: `GET /customers/abc`).
        - `default` → cualquier otra excepción no controlada.
    - Es preferible al encadenamiento de `if/instanceof` porque es más legible y evita casteos manuales.

#### 📌 En resumen

- Extendemos `AbstractErrorWebExceptionHandler` para personalizar el manejo global de errores en WebFlux.
- `@Order(-2)` asegura que nuestro handler se ejecute antes que el default de Spring.
- `Constructor con codecs` garantiza que las respuestas se serialicen correctamente.
- `getRoutingFunction(...)` registra una única ruta comodín con `RequestPredicates.all()`.
- `handleAllErrors(...)` centraliza el despacho usando `switch` con pattern matching.
- `Métodos específicos` + `buildResponse` permiten devolver errores consistentes en formato `ProblemDetail`.

### 💡 Con esto nuestra documentación cubre dos niveles de manejo de errores

- `Router-specific` con `.onError(...)`.
- `Global` con `AbstractErrorWebExceptionHandler`.

## 🧪 Probando Validación de Entradas con `.onError(...)`

Una vez configurado el `CustomerExceptionHandler` y las reglas de `.onError(...)` en el `CustomerRouter`, verificamos
que las excepciones se capturen correctamente y se devuelvan en formato estándar `ProblemDetail (RFC 7807)`.

### ❌ Cliente no existe

````bash
$ curl -v http://localhost:8080/api/v1/customers/99 | jq
>
< HTTP/1.1 404 Not Found
< Content-Type: application/problem+json
< Content-Length: 184
<
{
  "detail": "Cliente con id [99] no se encuentra",
  "instance": "/api/v1/customers/99",
  "status": 404,
  "title": "Cliente no encontrado",
  "type": "https://example.com/problems/customer-not-found"
}
````

✅ Aquí se ejecuta el método `.onError(...)` del `RouterFunction`, que captura la excepción
`CustomerNotFoundException`. Finalmente, se invoca `handleNotFoundException(...)` en la clase
`CustomerExceptionHandler`.

### ⚠️ Validación de datos de entrada

#### Caso 1: `name` vacío

````bash
$ curl -v -X POST -H "Content-Type: application/json" -d "{\"name\": \" \", \"email\": \"lesly@gmail.com\"}" http://localhost:8080/api/v1/customers | jq
>
< HTTP/1.1 400 Bad Request
< Content-Type: application/problem+json
< Content-Length: 160
<
{
  "detail": "El nombre es requerido",
  "instance": "/api/v1/customers",
  "status": 400,
  "title": "Entrada no válida",
  "type": "https://example.com/problems/invalid-input"
}
````

#### Caso 2: `email` inválido

````bash
$ curl -v -X POST -H "Content-Type: application/json" -d "{\"name\": \"Lesly\", \"email\": \"gmail.com\"}" http://localhost:8080/api/v1/customers | jq
>
< HTTP/1.1 400 Bad Request
< Content-Type: application/problem+json
< Content-Length: 180
<
{
  "detail": "Se requiere un correo electrónico válido",
  "instance": "/api/v1/customers",
  "status": 400,
  "title": "Entrada no válida",
  "type": "https://example.com/problems/invalid-input"
}
````

✅ En ambos casos, el `CustomerValidator` lanza una `InvalidInputException`. El router la captura con
`.onError(InvalidInputException.class, ...)` y delega a `handleInvalidInputException(...)` en
`CustomerExceptionHandler`.

### 📌 Conclusión

- El manejador de errores a nivel de router (`.onError(...)`) funciona correctamente.
- Las excepciones específicas (`CustomerNotFoundException`, `InvalidInputException`) se transforman en respuestas
  claras y consistentes en formato `ProblemDetail`.
- Esto asegura que el cliente de la API reciba mensajes de error estandarizados y fáciles de interpretar.

## 🧪 Probando `GlobalErrorHandler`

En la clase `GlobalErrorHandler` verificamos que las excepciones no manejadas en el router se capturen
correctamente y se devuelvan en formato estándar `ProblemDetail (RFC 7807)`.

### ⚠️ Manejo de errores de entrada inválida (`NumberFormatException`)

````bash
$ curl -v http://localhost:8080/api/v1/customers/abc | jq
>
* Request completely sent off
< HTTP/1.1 400 Bad Request
< Content-Type: application/problem+json
< Content-Length: 217
<
{
  "detail": "Se esperaba un valor numérico pero se recibió un valor no válido",
  "instance": "/api/v1/customers/abc",
  "status": 400,
  "title": "Entrada no válida",
  "type": "https://example.com/problems/invalid-number-format"
}
````

✅ El `GlobalErrorHandler` captura el `NumberFormatException` y devuelve un `400 Bad Request` con un `ProblemDetail`
claro. En la consola se registra el error:

````bash
2026-04-03T13:24:17.159-05:00 ERROR 320 --- [webflux-playground] [ctor-http-nio-2] d.m.app.sec08.error.GlobalErrorHandler   : For input string: "abc" 
````

### 💥 Manejo de errores genéricos (`500 Internal Server Error`)

Simulamos un error deteniendo el servidor de base de datos PostgreSQL:

````bash
$ curl -v http://localhost:8080/api/v1/customers/10 | jq
>
< HTTP/1.1 500 Internal Server Error
< Content-Type: application/problem+json
< Content-Length: 178
<
{
  "detail": "Failed to obtain R2DBC Connection",
  "instance": "/api/v1/customers/10",
  "status": 500,
  "title": "Internal Server Error",
  "type": "https://example.com/problems/internal-error"
}
````

✅ El `GlobalErrorHandler` captura la excepción genérica y devuelve un `500 Internal Server Error` con un
`ProblemDetail` estándar. En el log se observa:

````bash
2026-04-03T13:25:32.103-05:00 ERROR 320 --- [webflux-playground] [actor-tcp-nio-1] b.w.a.e.AbstractErrorWebExceptionHandler : [58acb3e1-2]  500 Server Error for HTTP GET "/api/v1/customers/10"
````

### 📌 Conclusión

- El `GlobalErrorHandler` funciona correctamente:
    - Captura errores de formato `(NumberFormatException)` y devuelve un `400 Bad Request`.
    - Captura errores genéricos (como fallos de conexión a la base de datos) y devuelve un `500 Internal Server Error`.
- Todas las respuestas siguen el formato `ProblemDetail (RFC 7807)`, asegurando consistencia y claridad para los
  clientes de la API.

## 📘 Resultados Paginados

En esta sección implementamos la **paginación de clientes** utilizando `RouterFunction`.  
La paginación es una técnica que permite dividir grandes volúmenes de datos en **bloques o páginas**,
facilitando el consumo de información y evitando sobrecargar al cliente o al servidor.

### 🛠️ Handler para resultados paginados

En la clase `CustomerHandler` se agregaron dos métodos:

- **Paginación simple (`getSimplePaginationCustomers`)**
    - Retorna únicamente la lista de clientes correspondiente a la página solicitada.
    - Si no se especifican parámetros, se usan valores por defecto: `page = 0` y `size = 5`.

- **Paginación avanzada (`getAdvancedPaginationCustomers`)**
    - Retorna un objeto `Page<CustomerResponse>` que incluye tanto los clientes como los **metadatos de paginación**
      (total de páginas, número de elementos, etc.), ideal para interfaces más completas.

Ambos métodos utilizan un helper privado `getIntQueryParam(...)` para extraer parámetros de la query string y evitar
duplicar código.

````java

@Slf4j
@RequiredArgsConstructor
@Component
public class CustomerHandler {

    private final CustomerService customerService;

    public Mono<ServerResponse> getSimplePaginationCustomers(ServerRequest request) {
        Integer pageNumber = this.getIntQueryParam(request, "page", 0);
        Integer pageSize = this.getIntQueryParam(request, "size", 5);
        return this.customerService.getAllCustomers(pageNumber, pageSize)
                .collectList()
                .flatMap(customerResponseList ->
                        ServerResponse.ok().bodyValue(customerResponseList));
    }

    public Mono<ServerResponse> getAdvancedPaginationCustomers(ServerRequest request) {
        Integer pageNumber = this.getIntQueryParam(request, "page", 0);
        Integer pageSize = this.getIntQueryParam(request, "size", 5);
        return this.customerService.getAllCustomers(PageRequest.of(pageNumber, pageSize))
                .flatMap(customerResponsePage ->
                        ServerResponse.ok().bodyValue(customerResponsePage));
    }

    /* other methods */

    private Integer getIntQueryParam(ServerRequest request, String param, int defaultValue) {
        return request.queryParam(param)
                .map(Integer::parseInt)
                .orElse(defaultValue);
    }
}
````

> 💡 `Ventaja`: este enfoque permite que los clientes consuman datos en bloques, reduciendo el tiempo de respuesta
> y mejorando la experiencia de usuario.

### 🌐 Definición de las rutas

En la clase `CustomerRouter` se añadieron dos rutas específicas para exponer los métodos de paginación:

- 📋 **Paginación simple:** `/api/v1/customers/simple-pagination`
- 📊 **Paginación avanzada:** `/api/v1/customers/advanced-pagination`

````java

@RequiredArgsConstructor
@Configuration
public class CustomerRouter {

    private static final String CUSTOMERS_URI = "/api/v1/customers";
    private static final String CUSTOMER_ID_PATH = "/{customerId}";

    private final CustomerExceptionHandler customerExceptionHandler;

    @Bean
    public RouterFunction<ServerResponse> customerRoutes(CustomerHandler handler) {
        return RouterFunctions.route()
                .GET(CUSTOMERS_URI, handler::allCustomers)
                .GET(CUSTOMERS_URI + "/simple-pagination", handler::getSimplePaginationCustomers)
                .GET(CUSTOMERS_URI + "/advanced-pagination", handler::getAdvancedPaginationCustomers)
                .GET(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::getCustomer)
                .POST(CUSTOMERS_URI, handler::saveCustomer)
                .PUT(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::updateCustomer)
                .DELETE(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::deleteCustomer)
                .onError(CustomerNotFoundException.class, this.customerExceptionHandler::handleNotFoundException)
                .onError(InvalidInputException.class, this.customerExceptionHandler::handleInvalidInputException)
                .build();
    }
}
````

#### 📌 Conclusión

- La paginación simple devuelve únicamente la lista de clientes.
- La paginación avanzada devuelve además metadatos útiles para construir interfaces más completas.
- Ambas rutas están integradas en el `CustomerRouter`, manteniendo la coherencia con el resto de endpoints CRUD.
- Este patrón mejora la eficiencia y escalabilidad de la API, ofreciendo una experiencia más fluida a los consumidores.

## 🧭 Orden de las Rutas en Functional Endpoints

Cuando definimos rutas en una aplicación basada en funciones con **Spring WebFlux**, el **orden en que se registran las
rutas es crítico**. A diferencia de los controladores tradicionales con `@RestController`, aquí el enrutamiento es
**funcional y secuencial**: Spring evalúa las rutas en el orden en que fueron agregadas,
y **la primera coincidencia gana**.

### 📋 Ejemplo de RouterFunction

Así hemos venido construyendo nuestro `CustomerRouter`:

````java

@RequiredArgsConstructor
@Configuration
public class CustomerRouter {

    private static final String CUSTOMERS_URI = "/api/v1/customers";
    private static final String CUSTOMER_ID_PATH = "/{customerId}";

    private final CustomerExceptionHandler customerExceptionHandler;

    @Bean
    public RouterFunction<ServerResponse> customerRoutes(CustomerHandler handler) {
        return RouterFunctions.route()
                .GET(CUSTOMERS_URI, handler::allCustomers)
                .GET(CUSTOMERS_URI + "/simple-pagination", handler::getSimplePaginationCustomers)
                .GET(CUSTOMERS_URI + "/advanced-pagination", handler::getAdvancedPaginationCustomers)
                .GET(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::getCustomer)
                /*... other routes ...*/
                .build();
    }
}
````

### ⚠️ Posible conflicto

Observemos estas dos rutas:

````bash
.GET("/api/v1/customers/simple-pagination", handler::getSimplePaginationCustomers)
.GET("/api/v1/customers/{customerId}", handler::getCustomer)
````

Ambas comparten el prefijo `/api/v1/customers`, pero una es estática y específica (`/simple-pagination`) y la otra
es dinámica (`/{customerId}`).

Si invertimos el orden:

````bash
.GET("/api/v1/customers/{customerId}", handler::getCustomer)
.GET("/api/v1/customers/simple-pagination", handler::getSimplePaginationCustomers)
````

Una petición como:

````bash
GET /api/v1/customers/simple-pagination
````

Será interpretada por Spring como si `simple-pagination` fuera un `customerId`.  
👉 `Resultado`: se invoca el handler incorrecto (`getCustomer(...)`), generando errores en la respuesta.

### ✅ ¿Cómo evitarlo?

La regla es sencilla:

- **Primero registrar rutas más específicas (estáticas).**
- **Después registrar rutas genéricas o dinámicas.**

Orden correcto:

````bash
.GET("/api/v1/customers/simple-pagination", handler::getSimplePaginationCustomers)
.GET("/api/v1/customers/{customerId}", handler::getCustomer)
````

### 🔎 Diferencia con `@RestController`

- Con `@RestController` → `El orden no importa`, Spring se encarga de resolver las rutas.
- Con `RouterFunction` → El orden `sí importa`, porque tú defines manualmente el flujo de resolución.

### 🧠 Conclusión

- El enrutamiento funcional en WebFlux es secuencial: la primera coincidencia gana.
- Siempre define primero las rutas más específicas y luego las dinámicas.
- Este detalle es clave para evitar ambigüedades y garantizar que cada petición llegue al handler correcto.

## 🧪 Probando Paginación de Resultados

La paginación nos permite dividir grandes volúmenes de datos en bloques más pequeños y manejables.  
En este caso, probamos los dos endpoints que implementamos: **paginación simple** y **paginación avanzada**.

### 1️⃣ Paginación Simple

````bash
$ curl -v -G --data "page=0&size=3" http://localhost:8080/api/v1/customers/simple-pagination | jq
>
< HTTP/1.1 200 OK
< Content-Type: application/json
< Content-Length: 143
<
[
  {
    "id": 1,
    "name": "sam",
    "email": "sam@gmail.com"
  },
  {
    "id": 2,
    "name": "mike",
    "email": "mike@gmail.com"
  },
  {
    "id": 3,
    "name": "jake",
    "email": "jake@gmail.com"
  }
]
````

✅ La **paginación simple** devuelve únicamente la lista de clientes solicitados, sin metadatos adicionales.
Esto es útil cuando el cliente solo necesita los datos y no requiere información sobre el total de páginas o elementos.

### 2️⃣ Paginación Avanzada

````bash
$ curl -v -G --data "page=0&size=3" http://localhost:8080/api/v1/customers/advanced-pagination | jq
>
< HTTP/1.1 200 OK
< Content-Type: application/json
< Content-Length: 457
<
{
  "content": [
    {
      "id": 1,
      "name": "sam",
      "email": "sam@gmail.com"
    },
    {
      "id": 2,
      "name": "mike",
      "email": "mike@gmail.com"
    },
    {
      "id": 3,
      "name": "jake",
      "email": "jake@gmail.com"
    }
  ],
  "empty": false,
  "first": true,
  "last": false,
  "number": 0,
  "numberOfElements": 3,
  "pageable": {
    "offset": 0,
    "pageNumber": 0,
    "pageSize": 3,
    "paged": true,
    "sort": {
      "empty": true,
      "sorted": false,
      "unsorted": true
    },
    "unpaged": false
  },
  "size": 3,
  "sort": {
    "empty": true,
    "sorted": false,
    "unsorted": true
  },
  "totalElements": 10,
  "totalPages": 4
}
````

✅ La **paginación avanzada** devuelve no solo los clientes, sino también **metadatos de paginación:**

- `totalElements`: número total de registros.
- `totalPages`: cantidad de páginas disponibles.
- `number`: índice de la página actual.
- `first` / `last`: indicadores de si estamos en la primera o última página.
- `pageable`: información detallada del request (`pageNumber`, `pageSize`, `offset`, etc.).

Esto es ideal para construir interfaces más completas, donde el cliente necesita saber cuántas páginas existen y
cómo navegar entre ellas.

## 🧪 Pruebas de Integración

Copiamos el test de la `sec05` en el directorio de test de la `sec08`. Esto lo hacemos porque no hemos
agregado nuevas funcionalidades, simplemente ahora nuestras apis, están implementadas con los `RouterFunctions`,
así que las pruebas desarrolladas en la `sec05` deberían funcionar sin problemas.

Sin embargo, aunque la lógica de negocio no cambió respecto a la sección `sec05`, ahora usamos
`ProblemDetail` para manejar errores, por lo que las respuestas de error ya no vienen vacías.

👉 Por ejemplo, en el test `customerNotFound()` solo validamos el **status HTTP** (`404 Not Found`),
aunque podríamos verificar también el cuerpo del `ProblemDetail`.

### ⚙️ Configuración del Test

La clase de pruebas utiliza:

- **`@SpringBootTest`** → Levanta el contexto completo de Spring Boot.
- **`@AutoConfigureWebTestClient`** → Inyecta un `WebTestClient` para realizar peticiones reactivas.
- **`WebTestClient`** → Cliente especializado para probar APIs reactivas en `WebFlux`.

````java

@Slf4j
@AutoConfigureWebTestClient
@SpringBootTest(properties = "section=sec08")
class CustomerControllerTest {

    @Autowired
    private WebTestClient client;

    @Test
    void allCustomers() {
        // given & when
        WebTestClient.ResponseSpec response = this.client.get()
                .uri("/api/v1/customers")
                .exchange();

        // then
        response.expectStatus().is2xxSuccessful()
                .expectHeader().contentType(MediaType.APPLICATION_JSON)
                .expectBodyList(CustomerResponse.class)
                .value(customerResponseList -> {
                    log.info("{}", customerResponseList);
                    assertThat(customerResponseList).isNotEmpty();
                })
                .hasSize(10);
    }

    @Test
    void getSimplePaginationCustomers() {
        // given & when
        WebTestClient.ResponseSpec response = this.client.get()
                .uri(uriBuilder -> uriBuilder
                        .path("/api/v1/customers/simple-pagination")
                        .queryParam("page", 2)
                        .queryParam("size", 2)
                        .build()
                )
                .exchange();

        // then
        response.expectStatus().is2xxSuccessful()
                .expectHeader().contentType(MediaType.APPLICATION_JSON)
                .expectBody()
                .consumeWith(result -> log.info("{}", new String(Objects.requireNonNull(result.getResponseBody()))))
                .jsonPath("$.length()").isEqualTo(2)
                .jsonPath("$[0].id").isEqualTo(5)
                .jsonPath("$[1].id").isEqualTo(6);
    }

    @Test
    void getAdvancedPaginationCustomers() {
        // given & when
        WebTestClient.ResponseSpec response = this.client.get()
                .uri(uriBuilder -> uriBuilder
                        .path("/api/v1/customers/advanced-pagination")
                        .queryParam("page", 0)
                        .queryParam("size", 3)
                        .build()
                )
                .exchange();

        // then
        response.expectStatus().is2xxSuccessful()
                .expectHeader().contentType(MediaType.APPLICATION_JSON)
                .expectBody()
                .consumeWith(result -> log.info("{}", new String(Objects.requireNonNull(result.getResponseBody()))))
                .jsonPath("$.content.length()").isEqualTo(3)        // Verificamos que la página tenga 3 elementos
                .jsonPath("$.content[0].id").isEqualTo(1)           // Primer cliente esperado
                .jsonPath("$.content[1].id").isEqualTo(2)           // Segundo cliente esperado
                .jsonPath("$.content[2].id").isEqualTo(3)           // Tercer cliente esperado
                .jsonPath("$.totalElements").isEqualTo(10)          // Total de registros en la tabla
                .jsonPath("$.totalPages").isEqualTo(4)              // Total de páginas calculadas
                .jsonPath("$.first").isEqualTo(true)                // Indicador de primera página
                .jsonPath("$.last").isEqualTo(false);               // No es la última página
    }

    @Test
    void customerById() {
        // given
        Long customerId = 1L;

        // when
        WebTestClient.ResponseSpec response = this.client.get()
                .uri("/api/v1/customers/{customerId}", customerId)
                .exchange();

        // then
        response.expectStatus().is2xxSuccessful()
                .expectHeader().contentType(MediaType.APPLICATION_JSON)
                .expectBody()
                .consumeWith(result -> log.info("{}", new String(Objects.requireNonNull(result.getResponseBody()))))
                .jsonPath("$.id").isEqualTo(customerId)
                .jsonPath("$.name").isEqualTo("sam")
                .jsonPath("$.email").isEqualTo("sam@gmail.com");
    }

    @Test
    void customerById2() {
        // given
        Long customerId = 1L;

        // when
        WebTestClient.ResponseSpec response = this.client.get()
                .uri("/api/v1/customers/{customerId}", customerId)
                .exchange();

        // then
        response.expectStatus().is2xxSuccessful()
                .expectHeader().contentType(MediaType.APPLICATION_JSON)
                .expectBody(CustomerResponse.class)
                .consumeWith(result -> {
                    CustomerResponse customerResponse = result.getResponseBody();
                    log.info("{}", customerResponse);
                    assertThat(customerResponse)
                            .isNotNull()
                            .extracting(CustomerResponse::id, CustomerResponse::name, CustomerResponse::email)
                            .containsExactly(customerId, "sam", "sam@gmail.com");
                });
    }

    @Test
    void createAndDeleteCustomer() {
        // given
        CustomerRequest request = new CustomerRequest("Karen", "kasari@gmail.com");

        // when
        WebTestClient.ResponseSpec response = this.client.post()
                .uri("/api/v1/customers")
                .contentType(MediaType.APPLICATION_JSON)      //<-- Request
                .accept(MediaType.APPLICATION_JSON)  //<-- Response
                .bodyValue(request)
                .exchange();

        // then
        CustomerResponse customerResponse = response.expectStatus().isCreated()
                .expectHeader().contentType(MediaType.APPLICATION_JSON)
                .expectBody(CustomerResponse.class)
                .returnResult()
                .getResponseBody();
        log.info("{}", customerResponse);

        assertThat(customerResponse)
                .isNotNull()
                .extracting(CustomerResponse::name, CustomerResponse::email)
                .containsExactly("Karen", "kasari@gmail.com");
        assertThat(customerResponse.id()).isNotNull();

        // cleanup
        this.client.delete()
                .uri("/api/v1/customers/{customerId}", customerResponse.id())
                .exchange()
                .expectStatus().isNoContent()
                .expectBody().isEmpty();
    }

    @Test
    void updateCustomer() {
        // given
        CustomerRequest request = new CustomerRequest("Noel", "noel@gmail.com");
        Long customerId = 10L;

        // when
        WebTestClient.ResponseSpec exchangeCreate = this.client.put()
                .uri("/api/v1/customers/{id}", customerId)
                .bodyValue(request)
                .exchange();

        // then
        exchangeCreate.expectStatus().is2xxSuccessful()
                .expectHeader().contentType(MediaType.APPLICATION_JSON)
                .expectBody()
                .consumeWith(result -> log.info("{}", new String(Objects.requireNonNull(result.getResponseBody()))))
                .jsonPath("$.id").isEqualTo(customerId)
                .jsonPath("$.name").isEqualTo("Noel")
                .jsonPath("$.email").isEqualTo("noel@gmail.com");
    }

    @Test
    void customerNotFound() {
        // get
        this.client.get()
                .uri("/api/v1/customers/{id}", 11)
                .exchange()
                .expectStatus().isNotFound();

        // delete
        this.client.delete()
                .uri("/api/v1/customers/{id}", 11)
                .exchange()
                .expectStatus().isNotFound();

        // put
        CustomerRequest request = new CustomerRequest("Noel", "noel@gmail.com");
        this.client.put()
                .uri("/api/v1/customers/{id}", 11)
                .bodyValue(request)
                .exchange()
                .expectStatus().isNotFound();
    }
}
````

### 📊 Resultados de la ejecución

````bash
D:\programming\spring\01.udemy\03.vinoth_selvaraj\spring-webflux-masterclass\projects\webflux-playground (feature/section-8)
$ mvn test -Dtest="*/sec08/*"
[INFO] Scanning for projects...
[INFO]
[INFO] ------------------< dev.magadiflo:webflux-playground >------------------
[INFO] Building webflux-playground 0.0.1-SNAPSHOT
[INFO]   from pom.xml
[INFO] --------------------------------[ jar ]---------------------------------
[INFO]
...
2026-04-06T12:03:15.721-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: Karen
2026-04-06T12:03:15.723-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: kasari@gmail.com
2026-04-06T12:03:15.739-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "customers" ("name", "email") VALUES ($1, $2) RETURNING "id"
2026-04-06T12:03:15.747-05:00  INFO 12792 --- [webflux-playground] [           main] d.m.app.sec08.CustomerControllerTest     : CustomerResponse[id=11, name=Karen, email=kasari@gmail.com]
2026-04-06T12:03:15.761-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 11
2026-04-06T12:03:15.763-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers".* FROM "customers" WHERE "customers"."id" = $1 LIMIT 2
2026-04-06T12:03:15.769-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 11
2026-04-06T12:03:15.769-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: DELETE FROM "customers" WHERE "customers"."id" = $1
2026-04-06T12:03:15.782-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 1
2026-04-06T12:03:15.783-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers".* FROM "customers" WHERE "customers"."id" = $1 LIMIT 2
2026-04-06T12:03:15.795-05:00  INFO 12792 --- [webflux-playground] [           main] d.m.app.sec08.CustomerControllerTest     : {"id":1,"name":"sam","email":"sam@gmail.com"}
2026-04-06T12:03:16.127-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers"."id", "customers"."name", "customers"."email" FROM "customers" LIMIT 2 OFFSET 4
2026-04-06T12:03:16.142-05:00  INFO 12792 --- [webflux-playground] [           main] d.m.app.sec08.CustomerControllerTest     : [{"id":5,"name":"sophia","email":"sophia@example.com"},{"id":6,"name":"liam","email":"liam@example.com"}]
2026-04-06T12:03:16.166-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers"."id", "customers"."name", "customers"."email" FROM "customers" LIMIT 3
2026-04-06T12:03:16.167-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.QUERY                : Executing query: SELECT COUNT(*) FROM "customers"
2026-04-06T12:03:16.190-05:00  INFO 12792 --- [webflux-playground] [           main] d.m.app.sec08.CustomerControllerTest     : {"content":[{"id":1,"name":"sam","email":"sam@gmail.com"},{"id":2,"name":"mike","email":"mike@gmail.com"},{"id":3,"name":"jake","email":"jake@gmail.com"}],"empty":false,"first":true,"last":false,"number":0,"numberOfElements":3,"pageable":{"offset":0,"pageNumber":0,"pageSize":3,"paged":true,"sort":{"empty":true,"sorted":false,"unsorted":true},"unpaged":false},"size":3,"sort":{"empty":true,"sorted":false,"unsorted":true},"totalElements":10,"totalPages":4}
2026-04-06T12:03:16.207-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 10
2026-04-06T12:03:16.208-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers".* FROM "customers" WHERE "customers"."id" = $1 LIMIT 2
2026-04-06T12:03:16.221-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: Noel
2026-04-06T12:03:16.221-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: noel@gmail.com
2026-04-06T12:03:16.222-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [2] to: 10
2026-04-06T12:03:16.222-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.QUERY                : Executing query: UPDATE "customers" SET "name" = $1, "email" = $2 WHERE "customers"."id" = $3
2026-04-06T12:03:16.228-05:00  INFO 12792 --- [webflux-playground] [           main] d.m.app.sec08.CustomerControllerTest     : {"id":10,"name":"Noel","email":"noel@gmail.com"}
2026-04-06T12:03:16.238-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 11
2026-04-06T12:03:16.239-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers".* FROM "customers" WHERE "customers"."id" = $1 LIMIT 2
2026-04-06T12:03:16.241-05:00 ERROR 12792 --- [webflux-playground] [actor-tcp-nio-1] d.m.a.s.h.CustomerExceptionHandler       : Cliente con id [11] no se encuentra
2026-04-06T12:03:16.257-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 11
2026-04-06T12:03:16.258-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers".* FROM "customers" WHERE "customers"."id" = $1 LIMIT 2
2026-04-06T12:03:16.259-05:00 ERROR 12792 --- [webflux-playground] [actor-tcp-nio-1] d.m.a.s.h.CustomerExceptionHandler       : Cliente con id [11] no se encuentra
2026-04-06T12:03:16.263-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 11
2026-04-06T12:03:16.264-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers".* FROM "customers" WHERE "customers"."id" = $1 LIMIT 2
2026-04-06T12:03:16.265-05:00 ERROR 12792 --- [webflux-playground] [actor-tcp-nio-1] d.m.a.s.h.CustomerExceptionHandler       : Cliente con id [11] no se encuentra
2026-04-06T12:03:16.274-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: 1
2026-04-06T12:03:16.274-05:00 DEBUG 12792 --- [webflux-playground] [actor-tcp-nio-1] io.r2dbc.postgresql.QUERY                : Executing query: SELECT "customers".* FROM "customers" WHERE "customers"."id" = $1 LIMIT 2
2026-04-06T12:03:16.278-05:00  INFO 12792 --- [webflux-playground] [           main] d.m.app.sec08.CustomerControllerTest     : CustomerResponse[id=1, name=sam, email=sam@gmail.com]
[INFO] Tests run: 8, Failures: 0, Errors: 0, Skipped: 0, Time elapsed: 5.250 s -- in dev.magadiflo.app.sec08.CustomerControllerTest
[INFO]
[INFO] Results:
[INFO]
[INFO] Tests run: 8, Failures: 0, Errors: 0, Skipped: 0
[INFO]
[INFO] ------------------------------------------------------------------------
[INFO] BUILD SUCCESS
[INFO] ------------------------------------------------------------------------
[INFO] Total time:  14.083 s
[INFO] Finished at: 2026-04-06T12:03:16-05:00
[INFO] ------------------------------------------------------------------------
````

✅ **Todos los tests pasaron correctamente.**  
Los logs muestran la ejecución de queries en `PostgreSQL` y la serialización de respuestas en formato JSON, confirmando
que la integración entre `WebFlux` + `R2DBC` + `RouterFunction` funciona de manera estable.

## 🔀 Múltiples RouterFunctions

Para mantener una mejor organización en el código, es posible definir **múltiples beans de tipo `RouterFunction`**
dentro de la configuración.  
Esto permite separar las rutas por tipo de operación HTTP o por contexto de negocio, mejorando la **legibilidad** y
el **mantenimiento** del proyecto.

### 📂 Ejemplo de separación por tipo de operación

En el siguiente ejemplo se definen dos beans:

1. **`customerRoutes1`** → Maneja rutas que usan métodos distintos a `GET` (`POST`, `PUT`, `DELETE`).  
   Incluye además el manejo de errores con `.onError(...)`.

2. **`customerRoutes2`** → Maneja exclusivamente las rutas `GET`.  
   No incluye manejo de errores, por lo que delega en el `GlobalErrorHandler`.

````java

@RequiredArgsConstructor
@Configuration
public class CustomerRouter {

    private static final String CUSTOMERS_URI = "/api/v1/customers";
    private static final String CUSTOMER_ID_PATH = "/{customerId}";

    private final CustomerExceptionHandler customerExceptionHandler;

    @Bean
    public RouterFunction<ServerResponse> customerRoutes1(CustomerHandler handler) {
        return RouterFunctions.route()
                .POST(CUSTOMERS_URI, handler::saveCustomer)
                .PUT(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::updateCustomer)
                .DELETE(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::deleteCustomer)
                .onError(CustomerNotFoundException.class, this.customerExceptionHandler::handleNotFoundException)
                .onError(InvalidInputException.class, this.customerExceptionHandler::handleInvalidInputException)
                .build();
    }

    @Bean
    public RouterFunction<ServerResponse> customerRoutes2(CustomerHandler handler) {
        return RouterFunctions.route()
                .GET(CUSTOMERS_URI, handler::allCustomers)
                .GET(CUSTOMERS_URI + "/simple-pagination", handler::getSimplePaginationCustomers)
                .GET(CUSTOMERS_URI + "/advanced-pagination", handler::getAdvancedPaginationCustomers)
                .GET(CUSTOMERS_URI + CUSTOMER_ID_PATH, handler::getCustomer)
                .build();
    }
}
````

### 🧪 Pruebas a las rutas del primer bean `(customerRoutes1)`

Estas rutas incluyen manejo de errores con `.onError(...)`, por lo que las respuestas son correctas tanto en casos
exitosos como en fallos.

- ✅ **Petición exitosa (actualización de cliente)**

````bash
$ curl -v -X PUT -H "Content-Type: application/json" -d "{\"name\": \"Melissa\", \"email\": \"meli@gmail.com\"}" http://localhost:8080/api/v1/customers/10 | jq
>
< HTTP/1.1 200 OK
< Content-Type: application/json
< Content-Length: 51
<
{
  "id": 10,
  "name": "Melissa",
  "email": "meli@gmail.com"
}
````

- ❌ **Petición con error (cliente no encontrado):**

````bash
$ curl -v -X PUT -H "Content-Type: application/json" -d "{\"name\": \"Melissa\", \"email\": \"meli@gmail.com\"}" http://localhost:8080/api/v1/customers/50 | jq
>
< HTTP/1.1 404 Not Found
< Content-Type: application/problem+json
< Content-Length: 184
<
{
  "detail": "Cliente con id [50] no se encuentra",
  "instance": "/api/v1/customers/50",
  "status": 404,
  "title": "Cliente no encontrado",
  "type": "https://example.com/problems/customer-not-found"
}
````

### 🧪 Pruebas a las rutas del segundo bean `(customerRoutes2)`

Estas rutas no incluyen `.onError(...)`, por lo que los errores son manejados por el `GlobalErrorHandler`.

- ✅ **Petición exitosa (cliente existente):**

````bash
$ curl -v http://localhost:8080/api/v1/customers/1 | jq
>
< HTTP/1.1 200 OK
< Content-Type: application/json
< Content-Length: 45
<
{
  "id": 1,
  "name": "sam",
  "email": "sam@gmail.com"
}
````

- ❌ **Petición con error (cliente no encontrado):**

````bash
$ curl -v http://localhost:8080/api/v1/customers/99 | jq
>
< HTTP/1.1 500 Internal Server Error
< Content-Type: application/problem+json
< Content-Length: 180
<
{
  "detail": "Cliente con id [99] no se encuentra",
  "instance": "/api/v1/customers/99",
  "status": 500,
  "title": "Internal Server Error",
  "type": "https://example.com/problems/internal-error"
}
````

### 📌 Conclusión

- Definir múltiples `RouterFunction` permite organizar mejor las rutas por tipo de operación o contexto.
- El primer bean maneja errores con `.onError(...)`, devolviendo respuestas específicas `(404 Not Found)`.
- El segundo bean delega en el `GlobalErrorHandler`, devolviendo un `500 Internal Server Error` en caso de error.
- Este patrón ofrece flexibilidad, pero requiere cuidado para mantener la consistencia en el manejo de errores.
