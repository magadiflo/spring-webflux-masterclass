# 🌊 Sección 10: Streaming

---

## ⚙️ Configuración inicial

- Creamos el directorio correspondiente a la sección `/sec10` en `webflux-playground`.
- En el archivo `application.yml` definimos la sección activa:
  ````yml
  section: sec10
  ````

---

## 📖 Introducción: Patrones de comunicación

En la siguiente imagen podemos observar los distintos patrones de comunicación entre un `cliente-servidor`:

- request ➝ response
- request ➝ streaming response
- streaming-request ➝ response
- Bidirectional-streaming

![01.png](assets/section-10/01.png)

El concepto de `streaming` en sistemas `cliente-servidor` se refiere a la capacidad de **enviar y recibir datos
de manera continua**, en lugar de limitarse al modelo tradicional de *request-response*.

En el contexto de `Spring WebFlux` y `Reactive Streams`, esto se logra gracias a los tipos `Mono` y `Flux`:

- `Mono<T>` → representa **0 o 1 elemento** (ideal para request-response clásico).
- `Flux<T>` → representa **0..N elementos** (ideal para flujos continuos de datos).

Estos tipos permiten implementar patrones de comunicación más avanzados, que hacen que las aplicaciones sean
interactivas, eficientes y escalables, especialmente en escenarios de tiempo real o alto volumen de datos.

### 🔑 Los cuatro patrones principales

### ↔️ 1. Request-Response (clásico)

Tradicionalmente, los sistemas han utilizado un modelo de comunicación `request-response`.
En este patrón, el cliente envía una solicitud al servidor backend y este responde con un único resultado. La mayoría
de nuestras aplicaciones web y `APIs REST` siguen este enfoque.

Sin embargo, gracias al tipo de Publisher `Flux`, en `WebFlux` podemos implementar patrones de comunicación más
avanzados y eficientes, especialmente útiles cuando trabajamos con flujos de datos en tiempo real.

### 🔄 2. Request-Stream Response

En este modelo, el cliente realiza una solicitud única al servidor, pero el servidor responde con múltiples elementos
emitidos como un flujo continuo de datos (stream).

Ejemplos de uso:

- Descarga de archivos en fragmentos (chunks).
- Aplicaciones como Uber: al reservar un viaje, podrías recibir actualizaciones periódicas como "el conductor está a 8
  km", "a 5 km", etc.
- Notificaciones en tiempo real desde el servidor.

Este patrón evita que el cliente tenga que hacer múltiples peticiones para recibir actualizaciones, ya que es el
`servidor quien envía los datos de forma proactiva`.

### 🔄 3. Streaming Request–Single Response

En este patrón, el cliente envía un flujo de datos al servidor, pero el servidor responde con una sola respuesta final,
una vez que ha recibido toda la información.

Ejemplos de uso:

- Carga de archivos divididos en bloques.
- Envío continuo de datos desde sensores (por ejemplo, frecuencia cardíaca desde un Apple Watch), donde el servidor
  puede dar una respuesta procesada tras recibir todos los datos.

### 🔄 4. Bidirectional Streaming

Este patrón combina los dos anteriores: tanto el cliente como el servidor envían y reciben flujos de datos
simultáneamente.

Ejemplos de uso:

- Aplicaciones de chat en tiempo real.
- Juegos multijugador en línea.
- Herramientas colaborativas como pizarras compartidas o editores de texto en tiempo real.

## 📚 JSON Array

El `JSON Array` es un formato ampliamente utilizado en el diseño de `APIs REST`.
Por ejemplo:

- En una petición `POST` para crear múltiples productos.
- En una petición `GET` para recuperar una lista de productos.

En ambos casos, es común emplear un `JSON Array`.

### ⚠️ Problema con grandes volúmenes de datos

El inconveniente surge cuando realizamos una petición `GET` para obtener, por ejemplo, los primeros mil productos.
La respuesta comienza con un corchete de apertura (`[`), seguido de objetos JSON `{...}` separados por comas,
y finaliza con un corchete de cierre (`]`).

Ejemplo:

````bash
[
  {"id": 123, "name": "Product A", ...},
  {"id": 234, "name": "Product B", ...},
  {"id": 345, "name": "Product C", ...},
  ...
]
````

### 🚨 Escenario de falla

Imaginemos que el servidor envía los datos uno por uno y justo en el producto `1000` ocurre una falla, impidiendo el
envío del corchete de cierre (`]`).

- El cliente no podrá analizar la respuesta correctamente.
- La estructura del `JSON Array` se rompe.
- Aunque el servidor haya entregado casi todos los productos, los datos se vuelven inutilizables.

Este es un caso de **todo o nada**: sin el cierre correcto, el procesamiento de los datos no es viable.

### 📉 Limitaciones del JSON Array

- Adecuado para conjuntos de datos **pequeños o bien estructurados.**
- Poco eficiente cuando se manejan **grandes volúmenes de información.**
- Vulnerable a fallas de transmisión: basta un error para invalidar toda la respuesta.

### ✅ Conclusión

El formato `JSON Array` funciona bien en escenarios simples, pero **no escala de manera eficiente** cuando se trata
de grandes cantidades de datos o transmisión en tiempo real.

Es aquí donde entra en juego `JSON Lines`, un formato más robusto y tolerante a fallos, ideal para streaming de datos.

## 📜 JSON Lines (NDJSON: Newline Delimited JSON)

El formato `JSON Lines` conocido también como `(NDJSON) Newline Delimited JSON o JSON delimitado por nueva línea`, se
estructura de la siguiente manera:

````bash
{"id": 123, "name": "Product A", ...}
{"id": 234, "name": "Product B", ...}
{"id": 345, "name": "Product C", ...}
...
````

### 📌 Características principales

- Cada línea representa un **objeto JSON autónomo.**
- No requiere un corchete de apertura/cierre como en `JSON Array`.
- Los datos pueden ser **leídos y procesados línea por línea,** lo que facilita el streaming.
- Es tolerante a fallos: si la transmisión se interrumpe, las líneas recibidas hasta ese momento siguen siendo válidas.

### 🚀 Ventajas frente a JSON Array

- **Procesamiento incremental:** el cliente puede consumir datos sin esperar a que se complete toda la respuesta.
- **Menor uso de memoria:** ideal para grandes volúmenes de información, ya que no es necesario cargar todo el
  conjunto en memoria.
- **Compatibilidad con streaming:** se integra naturalmente con `Flux` en `WebFlux`, permitiendo transmitir datos
  en tiempo real.

**💡 Ejemplo práctico:**  
Si un servidor transmite productos usando `JSON Lines`, el cliente puede empezar a mostrar resultados inmediatamente,
sin esperar a que se envíen los miles de productos completos.

### 📊 Comparación con JSON Array

| Aspecto                 | JSON Array                          | JSON Lines (NDJSON)                     |
|-------------------------|-------------------------------------|-----------------------------------------|
| **Estructura**          | Corchetes ``[`` ``]`` y comas       | Cada línea es un objeto JSON            |
| **Procesamiento**       | Todo o nada (requiere cierre)       | Incremental, línea por línea            |
| **Tolerancia a fallos** | Baja: un error invalida todo        | Alta: las líneas previas siguen válidas |
| **Uso de memoria**      | Elevado en grandes volúmenes        | Bajo, procesamiento en streaming        |
| **Escenarios ideales**  | Datos pequeños y bien estructurados | Datos masivos, tiempo real, IoT         |

### ✅ Conclusión

- `JSON Array` sigue siendo útil en escenarios donde los datos tienen una relación estrecha
  (ej. un producto con su lista de reseñas).
- Sin embargo, para **millones de registros** o transmisión en tiempo real, `JSON Lines` es una alternativa más
  eficiente y robusta.
- En aplicaciones reactivas con **Spring WebFlux**, `JSON Lines` se convierte en el formato ideal para implementar
  patrones de streaming.

## 🏗️ Construcción del proyecto de ejemplo

En este apartado vamos a construir nuestro proyecto creando el paquete `sec10` para poder jugar con el `streaming`.
La idea es simular escenarios con **millones de productos**, explorando operaciones de carga, descarga y transmisión de
datos en tiempo real.

Como lo mencioné anteriormente, en esta sección estaremos trabajando sobre el paquete `sec10` de nuestro proyecto
`webflux-playground`.

### 📦 Entidad `Product`

Creamos la entidad `Product` mapeada a la tabla `products`:

````java

@ToString
@AllArgsConstructor
@NoArgsConstructor
@Builder
@Setter
@Getter
@Table(name = "products")
public class Product {
    @Id
    private Long id;
    private String description;
    private Integer price;
}
````

### 📂 Repositorio `ProductRepository`

Definimos el repositorio reactivo para la entidad:

````java
public interface ProductRepository extends ReactiveCrudRepository<Product, Long> {
}
````

Este repositorio nos permitirá realizar operaciones CRUD de manera reactiva, aprovechando `Flux` y `Mono`.

### 📑 DTOs de producto

Es fundamental separar la entidad de los objetos que exponemos en la API. Creamos los DTOs para solicitudes y
respuestas:

````java
public record ProductRequest(String description,
                             Integer price) {
}

public record ProductResponse(Long id,
                              String description,
                              Integer price) {
}
````

### 🔄 Clase mapeadora `ProductMapper`

Creamos un componente para convertir entre entidad y DTO:

````java

@Component
public class ProductMapper {
    public Product toProduct(ProductRequest request) {
        return Product.builder()
                .description(request.description())
                .price(request.price())
                .build();
    }

    public ProductResponse toProductResponse(Product product) {
        return new ProductResponse(
                product.getId(),
                product.getDescription(),
                product.getPrice()
        );
    }
}
````

### ✅ Conclusión

Con estas clases iniciales (`Product`, `ProductRepository`, `DTOs` y `ProductMapper`) ya tenemos la base del modelo de
datos para la sección de `streaming`.
Esto nos permitirá simular escenarios de alta carga y transmisión continua, manteniendo una arquitectura limpia y
preparada para trabajar con `Flux` y `Mono`.

## 🗄️ Product Service

Creamos el servicio con dos métodos:

- Uno para guardar un `Flux` de productos.
- Otro para recuperar la cantidad de productos que tiene la base de datos.

````java
public interface ProductService {
    Flux<ProductResponse> saveProducts(Flux<ProductRequest> productRequestFlux);

    Mono<Long> countProducts();
}
````

````java

@RequiredArgsConstructor
@Service
public class ProductServiceImpl implements ProductService {

    private final ProductRepository productRepository;
    private final ProductMapper productMapper;

    @Override
    public Flux<ProductResponse> saveProducts(Flux<ProductRequest> productRequestFlux) {
        return productRequestFlux
                .map(this.productMapper::toProduct)
                .as(this.productRepository::saveAll)
                .map(this.productMapper::toProductResponse);
    }

    @Override
    public Mono<Long> countProducts() {
        return this.productRepository.count();
    }
}
````

### 📌 Explicación

- `saveProducts(Flux<ProductRequest>)`
    - Recibe un flujo de solicitudes `(Flux<ProductRequest)`.
    - Cada solicitud se transforma en entidad `Product` mediante el `ProductMapper`.
    - Se guarda de forma reactiva con `saveAll`.
    - Finalmente, se convierte en `ProductResponse` para devolver al cliente.
    - 👉 Este método demuestra cómo procesar y persistir datos en streaming.
- `countProducts()`
    - Devuelve un `Mono<Long>` con el total de productos en la base de datos.
    - Útil para verificar la cantidad de registros y validar escenarios de carga masiva.

### ✅ Conclusión

El `ProductService` abstrae la lógica de negocio y permite:

- Guardar productos en streaming de manera eficiente.
- Consultar el volumen de datos en la base de forma reactiva.

Esto nos prepara para los siguientes pasos, donde expondremos endpoints que aprovechen estos métodos para
**simular escenarios de streaming con millones de productos.**

## 📤 Product Streaming Upload API

Anteriormente, creamos dos DTOs:

- `ProductRequest`: para mapear los datos del request.
- `ProductResponse`: para retornar información hacia el cliente.

En este caso, como vamos a procesar un flujo de `ProductRequest`, a nuestro negocio le interesa **emitir un solo
resultado** luego de todo el procesamiento.

Normalmente, podríamos emitir un `Flux<ProductResponse>`, pero aquí buscamos emitir una sola respuesta en un `Mono`,
que informe al cliente la **cantidad de productos** que existen en la base de datos, acompañado de un identificador
único para simular una confirmación de carga.

````java
public record UploadResponse(UUID confirmationId,
                             Long productCount) {
}
````

### ⚙️ Controlador REST

Este código define un controlador REST en Spring Boot para manejar la carga de productos usando programación reactiva
con `Spring WebFlux`:

````java

@Slf4j
@RequiredArgsConstructor
@RestController
@RequestMapping(path = "/api/v1/products")
public class ProductController {

    private final ProductService productService;

    @PostMapping(path = "/upload", consumes = MediaType.APPLICATION_NDJSON_VALUE)
    public Mono<UploadResponse> uploadProducts(@RequestBody Flux<ProductRequest> productRequestFlux) {
        log.info("Iniciando el proceso de upload de products");

        return this.productService.saveProducts(productRequestFlux.doOnNext(productRequest -> log.info("ProductRequest: {}", productRequest)))
                .then(this.productService.countProducts())
                .map(count -> new UploadResponse(UUID.randomUUID(), count));
    }
}
````

#### 📌 Explicación

- `@PostMapping` con `consumes = MediaType.APPLICATION_NDJSON_VALUE`
    - Indica que el endpoint `/upload` acepta datos en formato `NDJSON` `(application/x-ndjson)`.
    - Cada objeto JSON está en una línea independiente, lo que facilita el procesamiento en **streaming.**
- **Flujo de trabajo del método** `uploadProducts`:
    1. Recibe un `Flux<ProductRequest>` desde el cuerpo de la petición.
    2. Cada request se procesa y se guarda en la base de datos.
    3. Una vez completado el flujo, se obtiene el conteo total de productos `(Mono<Long>)`.
    4. Se construye un `UploadResponse` con un `UUID` aleatorio y el conteo de productos.
- **Beneficio:** el cliente recibe una confirmación única, incluso si se enviaron miles de productos en streaming.

#### 🧪 Ejemplo de NDJSON

````bash
{"id": 123, "name": "Product A", ...}
{"id": 234, "name": "Product B", ...}
{"id": 345, "name": "Product C", ...}
````

Cada línea representa un objeto JSON autónomo, lo que permite leer y procesar cada uno de manera independiente.

### ✅ Conclusión

Este endpoint demuestra cómo `Spring WebFlux` puede manejar cargas masivas de datos en formato `NDJSON`,
procesándolos en streaming y devolviendo una **respuesta única y compacta** al cliente.

## 📡 Product Client - Upload

En el paquete de pruebas `src/test/java/dev/magadiflo/app/sec10` crearemos la clase `ProductClient`, que actuará
como un **cliente simulado.**

La idea es que este cliente represente a **“otro”** microservicio que desea llamar a nuestro endpoint
`/api/v1/products/upload` para subir productos. En lugar de construir un microservicio adicional, lo implementamos
dentro del paquete de pruebas para simplificar la validación del flujo.

De esta forma:

- Levantamos nuestro controlador `ProductController`.
- Desde el cliente de pruebas `(ProductClient)`, realizamos las llamadas como si fueran externas.
- Esto nos permite verificar el comportamiento del endpoint en condiciones similares a un entorno real.

### ⚙️ Implementación del cliente

````java
public class ProductClient {
    private final WebClient client = WebClient.builder()
            .baseUrl("http://localhost:8080")
            .build();

    public Mono<UploadResponse> uploadProducts(Flux<ProductRequest> productRequestFlux) {
        return this.client.post()
                .uri("/api/v1/products/upload")
                .contentType(MediaType.APPLICATION_NDJSON)
                .body(productRequestFlux, ProductRequest.class)
                .retrieve()
                .bodyToMono(UploadResponse.class);
    }
}
````

#### 📌 Explicación

- `WebClient`: se configura con la URL base `http://localhost:8080`, apuntando al servidor donde corre nuestro
  controlador.
- `uploadProducts(...)`:
    - Envía un `Flux<ProductRequest>` al endpoint `/api/v1/products/upload`.
    - Define el `contentType` como `application/x-ndjson` para transmitir datos en formato `NDJSON`.
    - Usa `.retrieve().bodyToMono(UploadResponse.class)` para recibir la respuesta única del servidor
      `(UploadResponse)`.

Este cliente permite simular la interacción entre microservicios de manera sencilla y probar el flujo de streaming
upload sin necesidad de levantar múltiples aplicaciones.

### ✅ Conclusión

- El `ProductClient` nos brinda una forma práctica de **probar la API de streaming** desde un entorno controlado
  de pruebas.
- Así validamos que el endpoint `/upload` funciona correctamente con datos en formato `NDJSON`, asegurando que el
  flujo de carga masiva se procese de manera reactiva.

## ⏫ Probando subida de productos streaming

### ▶️ Ejecutando clase principal

Para realizar nuestras pruebas vamos a ejecutar la clase principal `WebfluxPlaygroundApplication` para que el endpoint
de nuestro controlador `ProductController` esté en ejecución y disponible para recibir los request de subida de
productos.

Recordemos que estamos trabajando sobre el paquete `sec10` configurado en el `application.yml`.

### 🧪 Ejemplo 01 - Subida de 1 producto con retraso de 10 segundos

Para este primer ejemplo vamos a crear la clase `ProductsUploadDownloadTest` en nuestro paquete de pruebas de la
`sec10`, donde subiremos un único producto que tiene intencionalmente una demora de `10 segundos`.

````java

@Slf4j
class ProductsUploadDownloadTest {

    private final ProductClient productClient = new ProductClient();

    @Test
    void uploadProducts() {
        Flux<ProductRequest> productRequestFlux = Flux.just(new ProductRequest("iphone", 1000))
                .delayElements(Duration.ofSeconds(10));

        this.productClient.uploadProducts(productRequestFlux)
                .doOnNext(uploadResponse -> log.info("Recibido: {}", uploadResponse))
                .then()
                .as(StepVerifier::create)
                .verifyComplete();
    }
}
````

#### 📌 Explicación del flujo

1. **Se ejecuta el método de prueba:** se envía un único producto envuelto en un `Flux`.
2. **El endpoint recibe la solicitud inmediatamente:** aunque el producto aún no se emite, el log confirma que
   el controlador fue invocado:

````bash
2026-04-15T12:41:33.801-05:00  INFO 10112 --- [webflux-playground] [ctor-http-nio-3] d.m.a.s.controller.ProductController     : Iniciando el proceso de upload de products
````

3. **Retraso simulado con `delayElements(10s)`:** el producto se envía recién después de 10 segundos.
4. **Procesamiento en backend:** a las `12:41:43` se registra la llegada del producto y se ejecutan las operaciones
   de inserción y conteo en la base:

````bash
2026-04-15T12:41:43.896-05:00  INFO 10112 --- [webflux-playground] [ctor-http-nio-3] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=iphone, price=1000]
2026-04-15T12:41:43.897-05:00 DEBUG 10112 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: iphone
2026-04-15T12:41:43.897-05:00 DEBUG 10112 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 1000
2026-04-15T12:41:43.898-05:00 DEBUG 10112 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:41:43.899-05:00 DEBUG 10112 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: SELECT COUNT(*) FROM "products"
````

5. **Respuesta final recibida:** el backend devuelve la confirmación con un `UUID` y el conteo actualizado de productos:

````bash
12:41:43.982 [reactor-http-nio-2] INFO dev.magadiflo.app.sec10.ProductsUploadDownloadTest -- Recibido: UploadResponse[confirmationId=18ab38e1-b311-4aad-8f41-acf4a61056ac, productCount=12]
````

#### ✅ Conclusión

Este test demuestra cómo el endpoint de subida en streaming:

- **Recibe la solicitud inmediatamente,** incluso si los datos se emiten con retraso.
- **Procesa los productos de forma reactiva,** respetando el flujo del `Flux`.
- **Devuelve una respuesta única y compacta** `(UploadResponse)` con confirmación y conteo actualizado.

Esto valida que nuestro sistema maneja correctamente escenarios de latencia simulada y sigue siendo eficiente en el
procesamiento de datos en streaming.

### 🧪 Ejemplo 02 - Subida de 10 productos con retraso de 2 segundos por producto

En este segundo método de prueba, enviamos 10 productos, cada uno con una demora de 2 segundos entre emisiones.

````java

@Slf4j
class ProductsUploadDownloadTest {

    private final ProductClient productClient = new ProductClient();

    @Test
    void uploadProducts2() {
        Flux<ProductRequest> productRequestFlux = Flux.range(1, 10)
                .map(i -> new ProductRequest("product-" + i, i))
                .delayElements(Duration.ofSeconds(2));

        this.productClient.uploadProducts(productRequestFlux)
                .doOnNext(uploadResponse -> log.info("Recibido2: {}", uploadResponse))
                .then()
                .as(StepVerifier::create)
                .verifyComplete();
    }
}
````

#### 📌 Explicación del flujo

1. **Conexión inmediata:** al ejecutar la prueba, el endpoint recibe la solicitud instantáneamente,
   aunque los productos aún no se han emitido.

````bash
2026-04-15T12:58:10.671-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : Iniciando el proceso de upload de products
````

2. **Emisión progresiva:** cada producto se envía con un intervalo de 2 segundos gracias a
   `delayElements(Duration.ofSeconds(2))`.
    - El backend procesa cada producto conforme llega.
    - Se registran logs de inserción en la base de datos para cada uno `(INSERT INTO "products"...)`.
3. **Procesamiento reactivo:** el controlador recibe y guarda los productos uno por uno, manteniendo la conexión activa
   durante todo el flujo.

````bash
2026-04-15T12:58:12.674-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-1, price=1]
2026-04-15T12:58:12.718-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-1
2026-04-15T12:58:12.720-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 1
2026-04-15T12:58:12.737-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:14.667-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-2, price=2]
2026-04-15T12:58:14.670-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-2
2026-04-15T12:58:14.670-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 2
2026-04-15T12:58:14.671-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:16.669-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-3, price=3]
2026-04-15T12:58:16.670-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-3
2026-04-15T12:58:16.670-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 3
2026-04-15T12:58:16.671-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:18.677-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-4, price=4]
2026-04-15T12:58:18.678-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-4
2026-04-15T12:58:18.678-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 4
2026-04-15T12:58:18.678-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:20.673-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-5, price=5]
2026-04-15T12:58:20.674-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-5
2026-04-15T12:58:20.674-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 5
2026-04-15T12:58:20.674-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:22.673-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-6, price=6]
2026-04-15T12:58:22.674-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-6
2026-04-15T12:58:22.674-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 6
2026-04-15T12:58:22.674-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:24.674-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-7, price=7]
2026-04-15T12:58:24.675-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-7
2026-04-15T12:58:24.675-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 7
2026-04-15T12:58:24.675-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:26.676-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-8, price=8]
2026-04-15T12:58:26.678-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-8
2026-04-15T12:58:26.678-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 8
2026-04-15T12:58:26.678-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:28.678-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-9, price=9]
2026-04-15T12:58:28.679-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-9
2026-04-15T12:58:28.680-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 9
2026-04-15T12:58:28.680-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:30.680-05:00  INFO 604 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : ProductRequest: ProductRequest[description=product-10, price=10]
2026-04-15T12:58:30.681-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [0] to: product-10
2026-04-15T12:58:30.681-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.PARAM                : Bind parameter [1] to: 10
2026-04-15T12:58:30.682-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: INSERT INTO "products" ("description", "price") VALUES ($1, $2) RETURNING "id"
2026-04-15T12:58:30.693-05:00 DEBUG 604 --- [webflux-playground] [actor-tcp-nio-3] io.r2dbc.postgresql.QUERY                : Executing query: SELECT COUNT(*) FROM "products"
````

4. **Respuesta final:** al terminar la transmisión de los 10 productos, el backend devuelve un `UploadResponse` con un
   `UUID` de confirmación y el conteo actualizado de productos.

````bash
12:58:30.798 [reactor-http-nio-2] INFO dev.magadiflo.app.sec10.ProductsUploadDownloadTest -- Recibido2: UploadResponse[confirmationId=7f8f08ff-db30-44b6-afed-849a2e39c938, productCount=20]
````

#### ✅ Conclusión

- **El flujo reactivo permite que los datos se envíen progresivamente,** sin necesidad de cargar todo en memoria.
- **La conexión se establece de inmediato,** aunque los datos se transmitan con retardos `(delayElements())`.
- **El backend procesa cada producto conforme llega,** registrándolo en la base antes de enviar la respuesta final.

Este ejemplo demuestra cómo `Spring WebFlux` maneja eficientemente cargas más grandes en streaming, manteniendo la
conexión activa y procesando datos de manera incremental.

### 🧪 Ejemplo 03 - Subida de 1 millón de productos

Recordemos que en nuestro endpoint estábamos usando el siguiente código:

````bash
productRequestFlux.doOnNext(productRequest -> log.info("ProductRequest: {}", productRequest))
````

Este fragmento nos permite visualizar cada `ProductRequest` en consola, útil para pruebas pequeñas.
Sin embargo, al subir **1 millón de productos**, la consola se saturaría de mensajes, por lo que eliminamos
esa parte del log para evitar ruido excesivo.

#### ⚙️ Endpoint ajustado

````java

@Slf4j
@RequiredArgsConstructor
@RestController
@RequestMapping(path = "/api/v1/products")
public class ProductController {

    private final ProductService productService;

    @PostMapping(path = "/upload", consumes = MediaType.APPLICATION_NDJSON_VALUE)
    public Mono<UploadResponse> uploadProducts(@RequestBody Flux<ProductRequest> productRequestFlux) {
        log.info("Iniciando el proceso de upload de products");

        return this.productService.saveProducts(productRequestFlux)
                .then(this.productService.countProducts())
                .map(count -> new UploadResponse(UUID.randomUUID(), count));
    }
}
````

Además, desactivamos el logging detallado de la interacción con la base de datos en `application.yml` para evitar
saturación:

````yml
#logging:
#  level:
#    io.r2dbc.postgresql.QUERY: debug
#    io.r2dbc.postgresql.PARAM: debug
````

> 💡 **Nota:** Estos cambios solo lo hacemos para este `Ejemplo 03`, cuando finalicemos este apartado los
> revertiremos.

#### 🧪 Test de subida masiva

En la clase `ProductsUploadDownloadTest` definimos un nuevo método que genera 1 millón de productos usando
`Flux.range(1, 1_000_000)`:

````java

@Slf4j
class ProductsUploadDownloadTest {

    private final ProductClient productClient = new ProductClient();

    @Test
    void uploadProducts3() {
        Flux<ProductRequest> productRequestFlux = Flux.range(1, 1_000_000)
                .map(i -> new ProductRequest("product-" + i, i));

        this.productClient.uploadProducts(productRequestFlux)
                .doOnNext(uploadResponse -> log.info("Recibido3: {}", uploadResponse))
                .then()
                .as(StepVerifier::create)
                .verifyComplete();
    }
}
````

#### 📌 Ejecución

1. **Inicio de la aplicación:** ejecutamos `WebfluxPlaygroundApplication`.
2. **Ejecución del test `uploadProducts3()`:** se establece la conexión con el endpoint:
    ````bash
    2026-04-16T10:40:30.375-05:00  INFO 7696 --- [webflux-playground] [ctor-http-nio-2] d.m.a.s.controller.ProductController     : Iniciando el proceso de upload de products
    ````
3. **Tiempo de procesamiento:**
    - En este entorno, procesar el millón de productos tomó `11min 57seg`.
    - En pruebas anteriores, con otro proyecto, el tiempo fue de `4min 51seg`.
    - El tutor del curso reportó `13seg`, probablemente porque usaba una base en memoria `(H2)` y menos procesos en
      ejecución.  
      👉 Esto evidencia cómo el **entorno y la base de datos** influyen fuertemente en el rendimiento.

4. **Respuesta final:** el backend confirma la inserción de **1,000,010 productos** (los 10 adicionales provienen del
   `data.sql` cargado al inicio).

````bash
10:52:26.720 [reactor-http-nio-2] INFO dev.magadiflo.app.sec10.ProductsUploadDownloadTest -- Recibido3: UploadResponse[confirmationId=85c10373-d477-4b2d-932a-510d5e25059d, productCount=1000010]
````

#### ✅ Conclusión

- **Spring WebFlux maneja eficientemente cargas masivas en streaming**, incluso con millones de registros.
- El **rendimiento depende del entorno** (tipo de base de datos, recursos disponibles, procesos en ejecución).
- El formato **NDJSON** y el uso de `Flux` permiten transmitir y procesar datos de manera incremental, sin necesidad de
  cargar todo en memoria.
- Este ejemplo valida la **escalabilidad del enfoque reactivo** en escenarios de alta demanda.

## 📥 Product Streaming Download API

### 🔄 Servicio

Definimos en el servicio un método para recuperar todos los productos en formato reactivo:

````java
public interface ProductService {
    Flux<ProductResponse> findAllProducts();
    /* other methods */
}
````

El `findAllProducts()` devuelve un `Flux<ProductResponse>`, lo que permite transmitir los productos de manera
incremental, ideal para escenarios de streaming.

````java

@RequiredArgsConstructor
@Service
public class ProductServiceImpl implements ProductService {

    private final ProductRepository productRepository;
    private final ProductMapper productMapper;

    @Override
    public Flux<ProductResponse> findAllProducts() {
        return this.productRepository.findAll()
                .map(this.productMapper::toProductResponse);
    }
    /* other methods */
}
````

### ⚙️ Controlador REST

Creamos el endpoint de descarga en formato `NDJSON`:

````java

@Slf4j
@RequiredArgsConstructor
@RestController
@RequestMapping(path = "/api/v1/products")
public class ProductController {

    private final ProductService productService;

    @GetMapping(path = "/download", produces = MediaType.APPLICATION_NDJSON_VALUE)
    public Mono<ResponseEntity<Flux<ProductResponse>>> downloadProducts() {
        return Mono.fromSupplier(() -> ResponseEntity.ok(this.productService.findAllProducts()));
    }
}
````

- `@GetMapping` con `produces = MediaType.APPLICATION_NDJSON_VALUE`: indica que el endpoint `/download` devuelve datos
  en formato `NDJSON`.
- El método retorna un `Flux<ProductResponse>` dentro de un `ResponseEntity`, permitiendo que los productos se
  transmitan línea por línea.

### 🧪 Probando endpoint de descarga

Ejecutamos el endpoint con `curl`:

````bash
$ curl -v http://localhost:8080/api/v1/products/download
>
< HTTP/1.1 200 OK
< transfer-encoding: chunked
< Content-Type: application/x-ndjson
<
{"id":1,"description":"iphone 20","price":1000}
{"id":2,"description":"iphone 18","price":750}
{"id":3,"description":"ipad","price":800}
{"id":4,"description":"mac pro","price":3000}
...
````

- Cada línea representa un objeto JSON independiente.
- El cliente puede empezar a consumir los datos inmediatamente, sin esperar a que se complete toda la transmisión.
- Esto valida el patrón `Request ➝ Streaming Response`, donde el servidor envía datos de manera continua.

### ✅ Conclusión

- El **Product Streaming Download API** permite transmitir productos en formato **NDJSON**, optimizando el consumo de
  grandes volúmenes de datos.
- Gracias a `Flux`, los clientes reciben los datos de manera incremental y eficiente, ideal para aplicaciones en
  tiempo real.
- Este enfoque elimina la necesidad de cargar toda la información en memoria antes de enviarla, mejorando la
  escalabilidad y rendimiento.

## 📡 Product Client - Download

En la clase `ProductClient` del paquete de pruebas de la `sec10`, agregamos el método `downloadProducts()`
para llamar al endpoint de descarga que acabamos de implementar.

````java
public class ProductClient {
    private final WebClient client = WebClient.builder()
            .baseUrl("http://localhost:8080")
            .build();

    /* another method */

    public Flux<ProductResponse> downloadProducts() {
        return this.client.get()
                .uri("/api/v1/products/download")
                .accept(MediaType.APPLICATION_NDJSON)
                .retrieve()
                .bodyToFlux(ProductResponse.class);
    }
}
````

#### 📌 Explicación

- `WebClient.get()`: realiza una petición `GET` al endpoint `/api/v1/products/download`.
- `accept(MediaType.APPLICATION_NDJSON)`: indica que el cliente espera recibir datos en formato `NDJSON`.
- `bodyToFlux(ProductResponse.class)`: convierte la respuesta en un `Flux<ProductResponse>`, permitiendo consumir los
  productos de manera incremental, línea por línea.

Este método simula cómo otro microservicio o cliente externo podría consumir datos en streaming desde nuestro backend.

### ✅ Conclusión

Con este cliente de pruebas:

- Podemos **descargar productos en formato NDJSON** directamente desde el endpoint `/download`.
- El uso de `Flux<ProductResponse>` permite procesar los datos de manera reactiva e incremental, sin necesidad de
  esperar a que se complete toda la transmisión.
- Esto valida el patrón `Request ➝ Streaming Response`, asegurando que nuestro sistema está preparado para manejar
  grandes volúmenes de datos en tiempo real.

## 🧪 Ejemplo: Download de 1 millón de productos

### 📝 Clase auxiliar `FileWriter`

Para esta prueba, creamos una clase llamada `FileWriter` que nos permitirá escribir los registros descargados en un
archivo `.txt`. El código de esta clase nos la proporcionó el tutor del curso.

````java
// Solo para demostración
public class FileWriter {

    private final Path path;
    private BufferedWriter writer;

    private FileWriter(Path path) {
        this.path = path;
    }

    private void createFile() {
        try {
            this.writer = Files.newBufferedWriter(path);
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

    private void closeFile() {
        try {
            this.writer.close();
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

    // just for demo
    private void write(String content) {
        try {
            this.writer.write(content);
            this.writer.newLine();
            this.writer.flush();
        } catch (IOException e) {
            throw new RuntimeException(e);
        }
    }

    public static Mono<Void> create(Flux<String> flux, Path path) {
        var writer = new FileWriter(path);
        return flux.doOnNext(writer::write)
                .doFirst(writer::createFile)
                .doFinally(s -> writer.closeFile())
                .then();
    }

}
````

### 🧪 Test de descarga

En la clase `ProductsUploadDownloadTest`, creamos un método de prueba que descarga los productos y los guarda
en un archivo `products.txt`:

````java

@Slf4j
class ProductsUploadDownloadTest {

    private final ProductClient productClient = new ProductClient();

    @Test
    void downloadProducts() {
        this.productClient.downloadProducts()
                .map(Record::toString)
                .as(stringFlux -> FileWriter.create(stringFlux, Path.of("products.txt")))
                .as(StepVerifier::create)
                .verifyComplete();
    }
}
````

#### 💡 Nota importante

Antes de ejecutar la prueba de descarga:

- Debemos asegurarnos de tener el millón de productos cargados en la base de datos.
- Si no están presentes (lo más probable, ya que la aplicación reinicia los datos en cada ejecución),
  primero debemos correr el test de subida masiva (`uploadProducts3()`).
- Una vez finalizada la subida, sin detener la aplicación, ejecutamos el test de descarga.

### 📌 Resultados

Al ejecutar el test `downloadProducts()`:

- El proceso toma aproximadamente `27 segundos` en generar el archivo `products.txt` con el contenido de
  **1,000,010 productos** (los 10 adicionales provienen del `data.sql` inicial).
- El archivo contiene cada producto en formato `ProductResponse`, línea por línea:

````
ProductResponse[id=1, description=iphone 20, price=1000]
ProductResponse[id=2, description=iphone 18, price=750]
ProductResponse[id=3, description=ipad, price=800]
ProductResponse[id=4, description=mac pro, price=3000]
ProductResponse[id=5, description=apple watch, price=400]
ProductResponse[id=6, description=macbook air, price=1200]
ProductResponse[id=7, description=airpods pro, price=250]
ProductResponse[id=8, description=imac, price=2000]
ProductResponse[id=9, description=apple tv, price=200]
ProductResponse[id=10, description=homepod, price=300]
ProductResponse[id=11, description=product-1, price=1]
ProductResponse[id=12, description=product-2, price=2]
ProductResponse[id=13, description=product-3, price=3]
...
````

### ✅ Conclusión

- El **download en streaming** permite consumir grandes volúmenes de datos de manera eficiente.
- Gracias al formato `NDJSON`, los productos se procesan línea por línea, evitando saturar memoria.
- El uso de `Flux` y escritura incremental en archivo (`FileWriter`) demuestra cómo integrar `Spring WebFlux`
  con operaciones de persistencia externa (como archivos).
- Este enfoque valida la **escalabilidad y eficiencia** del patrón `Request ➝ Streaming Response` en escenarios de
  millones de registros.
